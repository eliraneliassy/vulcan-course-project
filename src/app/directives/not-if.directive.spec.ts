import { TemplateRef } from '@angular/core';
import { NotIfDirective } from './not-if.directive';

xdescribe('NotIfDirective', () => {
  it('should create an instance', () => {
    // const directive = new NotIfDirective();
    // expect(directive).toBeTruthy();
  });
});
